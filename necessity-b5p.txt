B5p necessity evidence (pre-fix code)
=====================================

1. Production source-shape guard against the pre-fix plugins.rs region:

   FAIL-BEFORE source guard: std::fs::write(, std::fs::rename(,
   std::fs::remove_dir_all(, std::fs::remove_dir(

   The old install/removal path therefore bypassed every named audited
   package protocol (I-1/I-2).

2. Seeded half-package with plugin.wasm present and manifest.json absent:

   FAIL-BEFORE wedge: install=ENOENT; uninstall=installed plugin manifest
   is unreadable; target_still_exists=true

   Thus neither install nor uninstall could recover the version without
   manual filesystem surgery. This is the exact crash-cut harm required by
   the packet's necessity gate.

Barrier honesty: a missing fsync is not reliably observable as a behavioral
failure on the ordinary test filesystem. The behavioral fail-before evidence
therefore covers the observable wedge, while the source-shape guard covers the
otherwise unobservable durability-barrier omission. No fake fsync repro is
claimed.
