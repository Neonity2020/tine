Harvest H necessity evidence
base: e19514f98cf1044391f3a3d2f09d2579275353ee

Tests were added or retargeted before product edits and produced these failures:

- Theme RMW: two deliberately overlapped installs completed with only
  page.tine.theme.concurrent-a in storage and installedThemes; theme B was lost.
- Lazy-genesis capsule: schema-4 bytes decoded through the fallback instead of
  being rejected by the current-only decoder.
- Lazy-genesis containing format: a manifest with schema_version=4 passed
  validate_manifest instead of invalidating the whole baseline.
- Projection forensic evidence: canonical schema-1 evidence decoded instead of
  returning ForensicBindingMismatch.
- Projection containing format: STORE_CLAIM_MAGIC was TINEPR6 and did not list
  TINEPR6 as a rejected previous family.
- Real core open: an activated store rewritten with a TINEPR6/version-6 claim
  reopened instead of yielding the ProtocolIncompatible no-mutation boundary.
- fast_commit header guard: the header did not distinguish the unwired
  FastLocalCommitter from its eight production-wired structural counters.

The import.rs two-phase header and
the_activation_half_of_this_module_is_not_read_only guard were already present
and green on the pinned base, so H did not duplicate or rewrite them.
