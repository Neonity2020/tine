SELECT COUNT(*) FROM (SELECT fo.entity_id FROM search_substring_fts sf JOIN search_fts_owners fo ON fo.rowid = sf.rowid WHERE sf.normalized_text MATCH ? AND fo.entity_type = 1 LIMIT ?)
SELECT b.block_id, b.page_id, b.collapsed, b.heading_level, t.content, k.marker, pl.priority, pl.scheduled, pl.deadline FROM blocks b JOIN block_text t ON t.block_id = b.block_id LEFT JOIN tasks k ON k.block_id = b.block_id LEFT JOIN block_planning pl ON pl.block_id = b.block_id WHERE b.block_id IN (?)
SELECT b.block_id, b.page_id, b.parent_block_id, bt.query_visible FROM blocks b LEFT JOIN block_text bt ON bt.block_id = b.block_id WHERE b.block_id IN (?)
SELECT b.block_id, b.parent_block_id, b.page_id, b.order_key, q.preorder, q.result_id, q.estimated_bytes, q.tag_count, q.property_count FROM query_block_results q LEFT JOIN blocks b ON b.block_id = q.block_id WHERE q.page_id = ? AND q.preorder > ? ORDER BY q.preorder LIMIT ?
SELECT block_id, page_id, preorder FROM query_block_results WHERE block_id IN (?)
SELECT o.owner_type, o.owner_id, o.page_id, o.name, o.normalized_name, o.value, o.ordinal, b.page_id FROM properties o LEFT JOIN blocks b ON o.owner_type = 1 AND b.block_id = o.owner_id ORDER BY o.owner_type, o.owner_id, o.name, o.ordinal
SELECT o.owner_type, o.owner_id, o.page_id, o.name, o.normalized_name, o.value, o.ordinal, b.page_id, p.path, p.name, p.text_kind FROM properties o LEFT JOIN blocks b ON o.owner_type = 1 AND b.block_id = o.owner_id LEFT JOIN pages p ON p.page_id = o.page_id WHERE o.normalized_name = ? AND o.owner_type = 0 ORDER BY o.owner_type, o.owner_id, o.name, o.ordinal
SELECT o.owner_type, o.owner_id, o.page_id, o.name, o.normalized_name, o.value, o.ordinal, b.page_id, p.path, p.name, p.text_kind FROM properties o LEFT JOIN blocks b ON o.owner_type = 1 AND b.block_id = o.owner_id LEFT JOIN pages p ON p.page_id = o.page_id WHERE o.normalized_name = ? ORDER BY o.owner_type, o.owner_id, o.name, o.ordinal
SELECT owner_id, name, value FROM properties WHERE owner_type = 1 AND owner_id IN (?) ORDER BY owner_id, ordinal
SELECT owner_id, page_id, name, value, ordinal FROM properties WHERE owner_type = 0 AND owner_id IN (?) ORDER BY owner_id, ordinal, name
SELECT owner_id, tag FROM tags WHERE owner_type = 1 AND owner_id IN (?) ORDER BY owner_id, ordinal
SELECT p.page_id, s.revision FROM pages p LEFT JOIN direct_source_revisions s ON s.page_id = p.page_id ORDER BY p.page_id
SELECT page_id, path FROM pages WHERE page_id IN (?)
SELECT page_id, path, name, text_kind FROM pages
SELECT parent_block_id, page_id, COUNT(*) FROM blocks WHERE parent_block_id IN (?) GROUP BY parent_block_id, page_id
SELECT preorder FROM query_block_results WHERE block_id = ? AND page_id = ?
WITH content_ranked AS MATERIALIZED (SELECT b.page_id, bt.query_visible AS matched_text, tine_query_rank(?, CAST(length(CAST(bt.query_visible AS BLOB)) AS TEXT) || ':' || bt.query_visible || b.query_visible_folded) AS content_key FROM (SELECT fo.entity_id AS block_id FROM search_substring_fts sf JOIN search_fts_owners fo ON fo.rowid = sf.rowid WHERE sf.normalized_text MATCH ? AND fo.entity_type = 1) c JOIN blocks b ON b.block_id = c.block_id JOIN block_text bt ON bt.block_id = b.block_id), content_choices AS (SELECT k.*, ROW_NUMBER() OVER (PARTITION BY k.page_id ORDER BY k.content_key, k.matched_text) AS content_choice FROM content_ranked k WHERE k.content_key IS NOT NULL), content AS MATERIALIZED (SELECT 1 AS match_source, 0 AS candidate_kind, p.page_id, p.name, p.text_kind, p.journal_day, p.path, k.matched_text, 0 AS source_kind, k.content_key AS global_key, p.path AS tie_key FROM content_choices k JOIN pages p ON p.page_id = k.page_id WHERE k.content_choice = 1), candidates AS (SELECT * FROM content WHERE global_key IS NOT NULL) SELECT c.match_source, c.candidate_kind, c.page_id, c.name, c.text_kind, c.journal_day, c.path, c.matched_text, c.source_kind, c.global_key FROM candidates c ORDER BY c.match_source, c.global_key, c.tie_key COLLATE BINARY LIMIT ?
WITH m(block_id, page_id, parent_block_id) AS MATERIALIZED (SELECT b.block_id, b.page_id, b.parent_block_id FROM blocks b WHERE (EXISTS (SELECT 1 FROM tasks t1 WHERE (t1.block_id = b.block_id AND t1.marker IN (?))) AND b.page_id IN (SELECT pg2.page_id FROM pages pg2 WHERE (pg2.journal_day IS NOT NULL AND pg2.journal_day >= ? AND pg2.journal_day <= ?)))), r(block_id, page_id) AS (SELECT m.block_id, m.page_id FROM m WHERE (m.parent_block_id IS NULL OR m.parent_block_id NOT IN (SELECT block_id FROM m))) SELECT r.block_id, r.page_id, p.name, p.text_kind, p.journal_day, p.path, q.page_id, q.preorder, q.result_id, q.estimated_bytes, q.tag_count, q.property_count, b.order_key, o.position, COUNT(*) OVER () FROM r LEFT JOIN query_block_results q ON q.block_id = r.block_id LEFT JOIN blocks b ON b.block_id = r.block_id LEFT JOIN pages p ON p.page_id = r.page_id LEFT JOIN query_page_order o ON o.page_id = r.page_id ORDER BY p.name COLLATE BINARY ASC, CASE p.text_kind WHEN 1 THEN 0 ELSE 1 END ASC, o.position ASC, q.preorder ASC
WITH m(block_id, page_id, parent_block_id) AS MATERIALIZED (SELECT b.block_id, b.page_id, b.parent_block_id FROM blocks b WHERE (b.page_id IN (SELECT pg1.page_id FROM pages pg1 WHERE pg1.page_id IN (SELECT pr2.owner_id FROM properties pr2 WHERE (pr2.normalized_name = ? AND pr2.owner_type = ?))) AND (NOT EXISTS (SELECT 1 FROM pages pg3 WHERE (pg3.page_id = b.page_id AND (EXISTS (SELECT 1 FROM properties pr4 WHERE (pr4.owner_id = pg3.page_id AND (pr4.normalized_name = ? AND pr4.owner_type = ?))) AND (SELECT COUNT(*) FROM property_atoms ac5 WHERE ac5.owner_type = ? AND ac5.owner_id = pg3.page_id AND ac5.normalized_name = ?) = ?)))))), r(block_id, page_id) AS (SELECT m.block_id, m.page_id FROM m WHERE (m.parent_block_id IS NULL OR m.parent_block_id NOT IN (SELECT block_id FROM m))) SELECT r.block_id, r.page_id, p.name, p.text_kind, p.journal_day, p.path, q.page_id, q.preorder, q.result_id, q.estimated_bytes, q.tag_count, q.property_count, b.order_key, o.position, COUNT(*) OVER () FROM r LEFT JOIN query_block_results q ON q.block_id = r.block_id LEFT JOIN blocks b ON b.block_id = r.block_id LEFT JOIN pages p ON p.page_id = r.page_id LEFT JOIN query_page_order o ON o.page_id = r.page_id ORDER BY p.name COLLATE BINARY ASC, CASE p.text_kind WHEN 1 THEN 0 ELSE 1 END ASC, o.position ASC, q.preorder ASC
WITH m(block_id, page_id, parent_block_id) AS MATERIALIZED (SELECT b.block_id, b.page_id, b.parent_block_id FROM blocks b WHERE b.block_id IN (SELECT a2.owner_id FROM property_atoms a2 WHERE (a2.normalized_name = ? AND a2.owner_type = ? AND a2.atom_key = ?))), r(block_id, page_id) AS (SELECT m.block_id, m.page_id FROM m WHERE (m.parent_block_id IS NULL OR m.parent_block_id NOT IN (SELECT block_id FROM m))) SELECT r.block_id, r.page_id, p.name, p.text_kind, p.journal_day, p.path, q.page_id, q.preorder, q.result_id, q.estimated_bytes, q.tag_count, q.property_count, b.order_key, o.position FROM r LEFT JOIN query_block_results q ON q.block_id = r.block_id LEFT JOIN blocks b ON b.block_id = r.block_id LEFT JOIN pages p ON p.page_id = r.page_id LEFT JOIN query_page_order o ON o.page_id = r.page_id ORDER BY o.position ASC, q.preorder ASC
WITH m(block_id, page_id, parent_block_id) AS MATERIALIZED (SELECT b.block_id, b.page_id, b.parent_block_id FROM blocks b WHERE b.block_id IN (SELECT a2.owner_id FROM property_atoms a2 WHERE (a2.normalized_name = ? AND a2.owner_type = ? AND a2.atom_key = ?))), r(block_id, page_id) AS (SELECT m.block_id, m.page_id FROM m WHERE (m.parent_block_id IS NULL OR m.parent_block_id NOT IN (SELECT block_id FROM m))) SELECT r.block_id, r.page_id, p.name, p.text_kind, p.journal_day, p.path, q.page_id, q.preorder, q.result_id, q.estimated_bytes, q.tag_count, q.property_count, b.order_key, o.position, COUNT(*) OVER () FROM r LEFT JOIN query_block_results q ON q.block_id = r.block_id LEFT JOIN blocks b ON b.block_id = r.block_id LEFT JOIN pages p ON p.page_id = r.page_id LEFT JOIN query_page_order o ON o.page_id = r.page_id ORDER BY p.name COLLATE BINARY ASC, CASE p.text_kind WHEN 1 THEN 0 ELSE 1 END ASC, o.position ASC, q.preorder ASC
WITH m(block_id, page_id, parent_block_id) AS MATERIALIZED (SELECT b.block_id, b.page_id, b.parent_block_id FROM blocks b WHERE b.block_id IN (SELECT r1.block_id FROM block_path_refs r1 WHERE r1.normalized_name = ?)), r(block_id, page_id) AS (SELECT m.block_id, m.page_id FROM m WHERE (m.parent_block_id IS NULL OR m.parent_block_id NOT IN (SELECT block_id FROM m))) SELECT r.block_id, r.page_id, p.name, p.text_kind, p.journal_day, p.path, q.page_id, q.preorder, q.result_id, q.estimated_bytes, q.tag_count, q.property_count, b.order_key, o.position, COUNT(*) OVER () FROM r LEFT JOIN query_block_results q ON q.block_id = r.block_id LEFT JOIN blocks b ON b.block_id = r.block_id LEFT JOIN pages p ON p.page_id = r.page_id LEFT JOIN query_page_order o ON o.page_id = r.page_id ORDER BY p.name COLLATE BINARY ASC, CASE p.text_kind WHEN 1 THEN 0 ELSE 1 END ASC, o.position ASC, q.preorder ASC
WITH m(block_id, page_id, parent_block_id) AS MATERIALIZED (SELECT b.block_id, b.page_id, b.parent_block_id FROM blocks b WHERE b.block_id IN (SELECT t1.block_id FROM tasks t1 WHERE t1.marker IN (?))), r(block_id, page_id) AS (SELECT m.block_id, m.page_id FROM m WHERE (m.parent_block_id IS NULL OR m.parent_block_id NOT IN (SELECT block_id FROM m))) SELECT r.block_id, r.page_id, p.name, p.text_kind, p.journal_day, p.path, q.page_id, q.preorder, q.result_id, q.estimated_bytes, q.tag_count, q.property_count, b.order_key, o.position FROM r LEFT JOIN query_block_results q ON q.block_id = r.block_id LEFT JOIN blocks b ON b.block_id = r.block_id LEFT JOIN pages p ON p.page_id = r.page_id LEFT JOIN query_page_order o ON o.page_id = r.page_id ORDER BY o.position ASC, q.preorder ASC
WITH m(block_id, page_id, parent_block_id) AS MATERIALIZED (SELECT b.block_id, b.page_id, b.parent_block_id FROM blocks b WHERE b.block_id IN (SELECT t1.block_id FROM tasks t1 WHERE t1.marker IN (?))), r(block_id, page_id) AS (SELECT m.block_id, m.page_id FROM m WHERE (m.parent_block_id IS NULL OR m.parent_block_id NOT IN (SELECT block_id FROM m))) SELECT r.block_id, r.page_id, p.name, p.text_kind, p.journal_day, p.path, q.page_id, q.preorder, q.result_id, q.estimated_bytes, q.tag_count, q.property_count, b.order_key, o.position, COUNT(*) OVER () FROM r LEFT JOIN query_block_results q ON q.block_id = r.block_id LEFT JOIN blocks b ON b.block_id = r.block_id LEFT JOIN pages p ON p.page_id = r.page_id LEFT JOIN query_page_order o ON o.page_id = r.page_id ORDER BY p.name COLLATE BINARY ASC, CASE p.text_kind WHEN 1 THEN 0 ELSE 1 END ASC, o.position ASC, q.preorder ASC
WITH m(block_id, page_id, parent_block_id) AS MATERIALIZED (SELECT b.block_id, b.page_id, b.parent_block_id FROM blocks b WHERE b.page_id IN (SELECT pg1.page_id FROM pages pg1 WHERE (pg1.journal_day IS NOT NULL AND pg1.journal_day >= ? AND pg1.journal_day <= ?))), r(block_id, page_id) AS (SELECT m.block_id, m.page_id FROM m WHERE (m.parent_block_id IS NULL OR m.parent_block_id NOT IN (SELECT block_id FROM m))) SELECT r.block_id, r.page_id, p.name, p.text_kind, p.journal_day, p.path, q.page_id, q.preorder, q.result_id, q.estimated_bytes, q.tag_count, q.property_count, b.order_key, o.position, COUNT(*) OVER () FROM r LEFT JOIN query_block_results q ON q.block_id = r.block_id LEFT JOIN blocks b ON b.block_id = r.block_id LEFT JOIN pages p ON p.page_id = r.page_id LEFT JOIN query_page_order o ON o.page_id = r.page_id ORDER BY p.name COLLATE BINARY ASC, CASE p.text_kind WHEN 1 THEN 0 ELSE 1 END ASC, o.position ASC, q.preorder ASC
WITH m(block_id, page_id, parent_block_id) AS MATERIALIZED (SELECT b.block_id, b.page_id, b.parent_block_id FROM blocks b WHERE b.page_id IN (SELECT pg1.page_id FROM pages pg1 WHERE (pg1.name_key >= ? AND pg1.name_key < ?))), r(block_id, page_id) AS (SELECT m.block_id, m.page_id FROM m WHERE (m.parent_block_id IS NULL OR m.parent_block_id NOT IN (SELECT block_id FROM m))) SELECT r.block_id, r.page_id, p.name, p.text_kind, p.journal_day, p.path, q.page_id, q.preorder, q.result_id, q.estimated_bytes, q.tag_count, q.property_count, b.order_key, o.position, COUNT(*) OVER () FROM r LEFT JOIN query_block_results q ON q.block_id = r.block_id LEFT JOIN blocks b ON b.block_id = r.block_id LEFT JOIN pages p ON p.page_id = r.page_id LEFT JOIN query_page_order o ON o.page_id = r.page_id ORDER BY p.name COLLATE BINARY ASC, CASE p.text_kind WHEN 1 THEN 0 ELSE 1 END ASC, o.position ASC, q.preorder ASC
WITH m(block_id, page_id, parent_block_id) AS MATERIALIZED (SELECT b.block_id, b.page_id, b.parent_block_id FROM blocks b WHERE b.page_id IN (SELECT pg1.page_id FROM pages pg1 WHERE pg1.page_id IN (SELECT a3.owner_id FROM property_atoms a3 WHERE (a3.normalized_name = ? AND a3.owner_type = ? AND a3.atom_key = ?)))), r(block_id, page_id) AS (SELECT m.block_id, m.page_id FROM m WHERE (m.parent_block_id IS NULL OR m.parent_block_id NOT IN (SELECT block_id FROM m))) SELECT r.block_id, r.page_id, p.name, p.text_kind, p.journal_day, p.path, q.page_id, q.preorder, q.result_id, q.estimated_bytes, q.tag_count, q.property_count, b.order_key, o.position, COUNT(*) OVER () FROM r LEFT JOIN query_block_results q ON q.block_id = r.block_id LEFT JOIN blocks b ON b.block_id = r.block_id LEFT JOIN pages p ON p.page_id = r.page_id LEFT JOIN query_page_order o ON o.page_id = r.page_id ORDER BY p.name COLLATE BINARY ASC, CASE p.text_kind WHEN 1 THEN 0 ELSE 1 END ASC, o.position ASC, q.preorder ASC
WITH m(block_id, page_id, parent_block_id) AS MATERIALIZED (SELECT b.block_id, b.page_id, b.parent_block_id FROM blocks b WHERE b.page_id IN (SELECT pg1.page_id FROM pages pg1 WHERE pg1.page_id IN (SELECT a3.owner_id FROM property_atoms a3 WHERE (a3.normalized_name = ? AND a3.owner_type = ? AND a3.atom_key IN (?))))), r(block_id, page_id) AS (SELECT m.block_id, m.page_id FROM m WHERE (m.parent_block_id IS NULL OR m.parent_block_id NOT IN (SELECT block_id FROM m))) SELECT r.block_id, r.page_id, p.name, p.text_kind, p.journal_day, p.path, q.page_id, q.preorder, q.result_id, q.estimated_bytes, q.tag_count, q.property_count, b.order_key, o.position, COUNT(*) OVER () FROM r LEFT JOIN query_block_results q ON q.block_id = r.block_id LEFT JOIN blocks b ON b.block_id = r.block_id LEFT JOIN pages p ON p.page_id = r.page_id LEFT JOIN query_page_order o ON o.page_id = r.page_id ORDER BY p.name COLLATE BINARY ASC, CASE p.text_kind WHEN 1 THEN 0 ELSE 1 END ASC, o.position ASC, q.preorder ASC
WITH m(block_id, page_id, parent_block_id) AS MATERIALIZED (SELECT b.block_id, b.page_id, b.parent_block_id FROM blocks b WHERE b.query_visible_folded LIKE ? ESCAPE '\'), r(block_id, page_id) AS (SELECT m.block_id, m.page_id FROM m WHERE (m.parent_block_id IS NULL OR m.parent_block_id NOT IN (SELECT block_id FROM m))) SELECT r.block_id, r.page_id, p.name, p.text_kind, p.journal_day, p.path, q.page_id, q.preorder, q.result_id, q.estimated_bytes, q.tag_count, q.property_count, b.order_key, o.position, COUNT(*) OVER () FROM r LEFT JOIN query_block_results q ON q.block_id = r.block_id LEFT JOIN blocks b ON b.block_id = r.block_id LEFT JOIN pages p ON p.page_id = r.page_id LEFT JOIN query_page_order o ON o.page_id = r.page_id ORDER BY p.name COLLATE BINARY ASC, CASE p.text_kind WHEN 1 THEN 0 ELSE 1 END ASC, o.position ASC, q.preorder ASC
WITH page_text_candidates(page_id, name, text_kind, journal_day, path, matched_text, source_kind, source_ordinal) AS (SELECT p.page_id, p.name, p.text_kind, p.journal_day, p.path, p.name, 0, -1 FROM pages p UNION ALL SELECT p.page_id, p.name, p.text_kind, p.journal_day, p.path, a.normalized_alias, 1, MIN(a.ordinal) FROM reference_alias_declarations a JOIN pages p ON p.page_id = a.source_page_id WHERE a.source_entity_type = 0 AND a.source_entity_id = a.source_page_id GROUP BY p.page_id, a.normalized_alias), ranked AS MATERIALIZED (SELECT c.*, tine_query_rank(?, c.matched_text) AS owner_key FROM page_text_candidates c), choices AS (SELECT r.*, ROW_NUMBER() OVER (PARTITION BY r.page_id ORDER BY r.owner_key, r.source_kind, r.source_ordinal) AS owner_choice FROM ranked r WHERE r.owner_key IS NOT NULL), physical AS MATERIALIZED (SELECT 0 AS match_source, 0 AS candidate_kind, w.page_id, w.name, w.text_kind, w.journal_day, w.path, w.matched_text, w.source_kind, tine_query_rank(?, CAST(length(CAST(w.name AS BLOB)) AS TEXT) || ':' || w.name || w.matched_text) AS global_key, w.path AS tie_key FROM choices w WHERE w.owner_choice = 1), real_identities(name_key) AS (SELECT name_key FROM pages UNION SELECT normalized_alias FROM reference_alias_declarations), reference_choices AS (SELECT r.raw_name, r.normalized_name, ROW_NUMBER() OVER (PARTITION BY r.normalized_name ORDER BY r.raw_name, r.normalized_name, r.source_page_id) AS name_choice FROM reference_postings r JOIN pages p ON p.page_id = r.source_page_id WHERE r.target_type = 0 AND r.reference_kind <= 4 AND NOT EXISTS (SELECT 1 FROM real_identities i WHERE i.name_key = r.normalized_name)), virtual AS MATERIALIZED (SELECT 0 AS match_source, 1 AS candidate_kind, NULL AS page_id, v.raw_name AS name, 0 AS text_kind, NULL AS journal_day, '' AS path, v.raw_name AS matched_text, 0 AS source_kind, tine_query_rank(?, CAST(length(CAST(v.raw_name AS BLOB)) AS TEXT) || ':' || v.raw_name || v.raw_name) AS global_key, v.normalized_name AS tie_key FROM reference_choices v WHERE v.name_choice = 1), candidates AS (SELECT * FROM physical WHERE global_key IS NOT NULL UNION ALL SELECT * FROM virtual WHERE global_key IS NOT NULL) SELECT c.match_source, c.candidate_kind, c.page_id, c.name, c.text_kind, c.journal_day, c.path, c.matched_text, c.source_kind, c.global_key FROM candidates c ORDER BY c.match_source, c.global_key, c.tie_key COLLATE BINARY LIMIT ?
WITH page_text_candidates(page_id, name, text_kind, journal_day, path, matched_text, source_kind, source_ordinal) AS (SELECT p.page_id, p.name, p.text_kind, p.journal_day, p.path, p.name, 0, -1 FROM pages p UNION ALL SELECT p.page_id, p.name, p.text_kind, p.journal_day, p.path, a.normalized_alias, 1, MIN(a.ordinal) FROM reference_alias_declarations a JOIN pages p ON p.page_id = a.source_page_id WHERE a.source_entity_type = 0 AND a.source_entity_id = a.source_page_id GROUP BY p.page_id, a.normalized_alias), ranked AS MATERIALIZED (SELECT c.*, tine_query_rank(?, c.matched_text) AS owner_key FROM page_text_candidates c), choices AS (SELECT r.*, ROW_NUMBER() OVER (PARTITION BY r.page_id ORDER BY r.owner_key, r.source_kind, r.source_ordinal) AS owner_choice FROM ranked r WHERE r.owner_key IS NOT NULL), physical AS MATERIALIZED (SELECT 0 AS match_source, 0 AS candidate_kind, w.page_id, w.name, w.text_kind, w.journal_day, w.path, w.matched_text, w.source_kind, tine_query_rank(?, CAST(length(CAST(w.name AS BLOB)) AS TEXT) || ':' || w.name || w.matched_text) AS global_key, w.path AS tie_key FROM choices w WHERE w.owner_choice = 1), real_identities(name_key) AS (SELECT name_key FROM pages UNION SELECT normalized_alias FROM reference_alias_declarations), reference_choices AS (SELECT r.raw_name, r.normalized_name, ROW_NUMBER() OVER (PARTITION BY r.normalized_name ORDER BY r.raw_name, r.normalized_name, r.source_page_id) AS name_choice FROM reference_postings r JOIN pages p ON p.page_id = r.source_page_id WHERE r.target_type = 0 AND r.reference_kind <= 4 AND NOT EXISTS (SELECT 1 FROM real_identities i WHERE i.name_key = r.normalized_name)), virtual AS MATERIALIZED (SELECT 0 AS match_source, 1 AS candidate_kind, NULL AS page_id, v.raw_name AS name, 0 AS text_kind, NULL AS journal_day, '' AS path, v.raw_name AS matched_text, 0 AS source_kind, tine_query_rank(?, CAST(length(CAST(v.raw_name AS BLOB)) AS TEXT) || ':' || v.raw_name || v.raw_name) AS global_key, v.normalized_name AS tie_key FROM reference_choices v WHERE v.name_choice = 1), content_ranked AS MATERIALIZED (SELECT b.page_id, bt.query_visible AS matched_text, tine_query_rank(?, CAST(length(CAST(bt.query_visible AS BLOB)) AS TEXT) || ':' || bt.query_visible || b.query_visible_folded) AS content_key FROM (SELECT fo.entity_id AS block_id FROM search_substring_fts sf JOIN search_fts_owners fo ON fo.rowid = sf.rowid WHERE sf.normalized_text MATCH ? AND fo.entity_type = 1) c JOIN blocks b ON b.block_id = c.block_id JOIN block_text bt ON bt.block_id = b.block_id), content_choices AS (SELECT k.*, ROW_NUMBER() OVER (PARTITION BY k.page_id ORDER BY k.content_key, k.matched_text) AS content_choice FROM content_ranked k WHERE k.content_key IS NOT NULL AND NOT EXISTS (SELECT 1 FROM physical x WHERE x.page_id = k.page_id)), content AS MATERIALIZED (SELECT 1 AS match_source, 0 AS candidate_kind, p.page_id, p.name, p.text_kind, p.journal_day, p.path, k.matched_text, 0 AS source_kind, k.content_key AS global_key, p.path AS tie_key FROM content_choices k JOIN pages p ON p.page_id = k.page_id WHERE k.content_choice = 1), candidates AS (SELECT * FROM physical WHERE global_key IS NOT NULL UNION ALL SELECT * FROM virtual WHERE global_key IS NOT NULL UNION ALL SELECT * FROM content WHERE global_key IS NOT NULL) SELECT c.match_source, c.candidate_kind, c.page_id, c.name, c.text_kind, c.journal_day, c.path, c.matched_text, c.source_kind, c.global_key FROM candidates c ORDER BY c.match_source, c.global_key, c.tie_key COLLATE BINARY LIMIT ?
WITH r(page_id, name, text_kind, journal_day, path) AS (SELECT p.page_id, p.name, p.text_kind, p.journal_day, p.path FROM pages p WHERE (p.page_id IN (SELECT pr1.owner_id FROM properties pr1 WHERE (pr1.normalized_name = ? AND pr1.owner_type = ?)) AND (NOT (EXISTS (SELECT 1 FROM properties pr2 WHERE (pr2.owner_id = p.page_id AND (pr2.normalized_name = ? AND pr2.owner_type = ?))) AND (SELECT COUNT(*) FROM property_atoms ac3 WHERE ac3.owner_type = ? AND ac3.owner_id = p.page_id AND ac3.normalized_name = ?) = ?)))) SELECT r.page_id, r.name, r.text_kind, r.journal_day, r.path, o.position, q.estimated_bytes, q.property_count, COUNT(*) OVER () FROM r LEFT JOIN query_page_order o ON o.page_id = r.page_id LEFT JOIN query_page_results q ON q.page_id = r.page_id ORDER BY o.position LIMIT ?
WITH ranked AS MATERIALIZED (SELECT b.block_id, b.page_id, b.parent_block_id, b.order_key, bt.query_visible, p.name, p.text_kind, p.path, q.page_id AS result_page_id, q.preorder, q.result_id, q.estimated_bytes, q.tag_count, q.property_count, CASE WHEN bt.block_id IS NULL THEN zeroblob(1) ELSE tine_query_rank(?, CAST(length(CAST(bt.query_visible AS BLOB)) AS TEXT) || ':' || bt.query_visible || b.query_visible_folded) END AS rank_key, CASE WHEN bt.block_id IS NULL THEN 1 ELSE 0 END AS missing_text FROM (SELECT fo.entity_id AS block_id FROM search_substring_fts sf JOIN search_fts_owners fo ON fo.rowid = sf.rowid WHERE sf.normalized_text MATCH ? AND fo.entity_type = 1) c JOIN blocks b ON b.block_id = c.block_id LEFT JOIN block_text bt ON bt.block_id = b.block_id LEFT JOIN pages p ON p.page_id = b.page_id LEFT JOIN query_block_results q ON q.block_id = b.block_id) SELECT r.block_id, r.page_id, r.parent_block_id, r.order_key, r.query_visible, r.name, r.text_kind, r.path, r.result_page_id, r.preorder, r.result_id, r.estimated_bytes, r.tag_count, r.property_count, r.rank_key FROM ranked r WHERE r.missing_text = 1 OR r.rank_key IS NOT NULL ORDER BY r.missing_text DESC, r.rank_key, r.path COLLATE BINARY, r.preorder LIMIT ?
WITH requested(page_id) AS (VALUES (?)) SELECT o.owner_type, o.owner_id, o.page_id, o.name, o.normalized_name, o.value, o.ordinal, b.page_id, p.path, p.name, p.text_kind, requested.page_id, p.page_id FROM requested LEFT JOIN pages p ON p.page_id = requested.page_id LEFT JOIN properties o ON o.page_id = requested.page_id LEFT JOIN blocks b ON o.owner_type = 1 AND b.block_id = o.owner_id
