Managed storage saves now avoid repeated projection work, large pages recover reliably, and fresh Android devices can join from shared storage or return to Direct Files. This release also fixes atomic Sheet edits, Logseq-compatible block moves and nested drops, rename collisions, property autocomplete, display-math paste, block embeds, Windows updates, color emoji, custom Inter features, image lightboxes, task-marker clicks, and background-tab pane ownership.
